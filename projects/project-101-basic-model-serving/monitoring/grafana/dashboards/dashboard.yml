apiVersion: 1

providers:
  - name: 'ML API Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: true

# TODO: Create actual dashboard JSON files
# Example: ml-api-dashboard.json
# - Request rate, latency, error rate
# - Model inference time
# - GPU/CPU utilization
# - Memory usage
